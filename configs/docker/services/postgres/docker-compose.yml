# VDE ARCHITECTURAL RECORD
# @shared-law (Forge Component)
# Template for service VMs
# Variables: postgres, 2404, 5432, -, zsh /vde/scripts/setup/postgres-init.zsh
name: vde-postgres
services:
  vde-postgres:
    build:
      context: ../../../..
      dockerfile: configs/docker/vde-lang.Dockerfile
      args:
        VM_NAME: postgres
        PKGS_TO_INSTALL: "-"
        CUSTOM_BUILD_CMD: "zsh /vde/scripts/setup/postgres-init.zsh"
        POSTGRES_DEV_PASSWORD: "${POSTGRES_DEV_PASSWORD}"
        USERNAME: devuser
        UID: 1000
        GID: 1000
    image: vde-postgres:latest
    container_name: vde-postgres
    hostname: vde-postgres
    restart: unless-stopped

    ports:
      - "2404:2404"
      - "5432:5432"

    volumes:
      - ../../../../:/vde:ro
      - ../../../../data/postgres:/data
      - ../../../../logs/postgres:/logs
      - ~/.ssh/vde/vde_student.pub:/home/devuser/.ssh/vde/authorized_keys:ro
      - ~/.ssh/vde/vde_student:/home/devuser/.ssh/vde/vde_student:ro
      # Persistence mapping
      - ../../../../data/postgres/pgdata:/var/lib/postgresql/data
      # Docker Socket Sovereignty (Physical Verification Law)
      - /var/run/docker.sock:/var/run/docker.sock
      # macOS Canonical Bridge (Hardened to use explicit host socket)
      - ${VDE_HOST_SSH_AUTH_SOCK:-/run/host-services/ssh-auth.sock}:/run/host-services/ssh-auth.sock
      # VDE 2026 Sovereign Bridge
      - ${VDE_HOST_SSH_AUTH_SOCK:-/run/host-services/ssh-auth.sock}:/run/vde-ssh.sock

    environment:
      - SSH_AUTH_SOCK=/run/vde-ssh.sock
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
      - REDIS_ARGS=--save 60 1 --loglevel warning

    env_file:
      - ../../../../env-files/postgres.env

    networks:
      vde-net:
        aliases:
          - "postgres"
          - "vde-postgres"

    extra_hosts:
      - "vde-host:host-gateway"
      - "host:host-gateway"

    labels:
      - "vde.managed=true"
      - "vde.type=service"
      - "vde.name=postgres"

    healthcheck:
      test: ["CMD-SHELL", "if [ \"postgres\" = \"mysql\" ]; then mysqladmin ping -h localhost; elif command -v pg_isready >/dev/null; then pg_isready -U postgres; elif command -v redis-cli >/dev/null; then redis-cli ping; else exit 0; fi"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

networks:
  vde-net:
    external: true
