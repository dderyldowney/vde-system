# Template for language VMs
# Variables: flutter, 2205, apt-get update -y && apt-get install -y curl git unzip xz-utils zip libglu1-mesa && su devuser -c 'git clone --depth 1 https://github.com/flutter/flutter.git /home/devuser/flutter && export PATH="$PATH:/home/devuser/flutter/bin" && flutter precache'
name: vde-flutter
services:
  vde-flutter:
    build:
      context: ../../..
      dockerfile: configs/docker/flutter/Dockerfile
      args:
        USERNAME: devuser
        UID: 1000
        GID: 1000
        PUBLIC_KEYS_DIR: /public-ssh-keys
    image: vde-flutter:latest
    container_name: vde-flutter
    hostname: vde-flutter
    restart: unless-stopped
    labels:
      - "vde.managed=true"
      - "vde.type=lang"
      - "vde.name=vde-flutter"
    command: /usr/sbin/sshd -D

    ports:
      - "2205:22"

    volumes:
      - ../../../projects/flutter:/home/devuser/workspace
      - ../../../logs/flutter:/logs
      - ../../../public-ssh-keys:/public-ssh-keys:ro
      # SSH agent forwarding for VM->VM, VM->Host, VM->External communication
      - ${SSH_AUTH_SOCK:-/tmp/ssh-agent.sock}:/ssh-agent/sock:ro

    environment:
      - SSH_AUTH_SOCK=/ssh-agent/sock

    env_file:
      - ../../../env-files/vde-flutter.env

    networks:
      - vde-net

networks:
  vde-net:
    external: true
