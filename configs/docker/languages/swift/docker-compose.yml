# Template for language VMs
# Variables: swift, 2218, , zsh /vde/scripts/setup/swift-init.zsh
name: vde-swift
services:
  vde-swift:
    build:
      context: ../../../..
      dockerfile: configs/docker/vde-lang.Dockerfile
      args:
        VM_NAME: swift
        PKGS_TO_INSTALL: ""
        CUSTOM_BUILD_CMD: "zsh /vde/scripts/setup/swift-init.zsh"
        USERNAME: devuser
        UID: 1000
        GID: 1000
    image: vde-swift:latest
    container_name: vde-swift
    hostname: vde-swift
    restart: unless-stopped
    labels:
      - "vde.managed=true"
      - "vde.type=lang"
      - "vde.name=swift"
    command: ["/usr/local/bin/vde-entrypoint.zsh"]

    ports:
      - "2218:2218"

    volumes:
      - ../../../../:/vde:ro
      - ../../../../projects/swift:/home/devuser/workspace
      - ../../../../logs/swift:/logs
      - ~/.ssh/vde/vde_student.pub:/home/devuser/.ssh/vde/authorized_keys:ro
      - ~/.ssh/vde/vde_student:/home/devuser/.ssh/vde/vde_student:ro
      # Docker Socket Sovereignty (Physical Verification Law)
      - /var/run/docker.sock:/var/run/docker.sock
      # macOS Canonical Bridge (Hardened to use explicit host socket)
      - ${SSH_AUTH_SOCK}:/run/host-services/ssh-auth.sock
      # VDE 2026 Sovereign Bridge
      - ${SSH_AUTH_SOCK}:/run/vde-ssh.sock

    environment:
      - SSH_AUTH_SOCK=/home/devuser/.ssh/vde/agent.sock

    env_file:
      - ../../../../env-files/swift.env

    networks:
      - vde-net

networks:
  vde-net:
    external: true
