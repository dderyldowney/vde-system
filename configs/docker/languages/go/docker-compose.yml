# VDE ARCHITECTURAL RECORD
# @shared-law (Sovereign Law)
# Template for language VMs
# Variables: go, 2208, , zsh /vde/scripts/setup/go-init.zsh
name: vde-go
services:
  vde-go:
    build:
      context: ../../../..
      dockerfile: configs/docker/vde-lang.Dockerfile
      args:
        VM_NAME: go
        PKGS_TO_INSTALL: ""
        CUSTOM_BUILD_CMD: "zsh /vde/scripts/setup/go-init.zsh"
        USERNAME: devuser
        UID: 1000
        GID: 1000
    image: vde-go:latest
    container_name: vde-go
    hostname: vde-go
    restart: unless-stopped
    labels:
      - "vde.managed=true"
      - "vde.type=lang"
      - "vde.name=go"

    ports:
      - "2208:2208"

    volumes:
      - ../../../../:/vde:ro
      - ../../../../projects/go:/home/devuser/workspace
      - ../../../../logs/go:/logs
      # Docker Socket Sovereignty (Physical Verification Law)
      - /var/run/docker.sock:/var/run/docker.sock
      # macOS Canonical Bridge (Hardened to use explicit host socket)
      - ${VDE_HOST_SSH_AUTH_SOCK:-/run/host-services/ssh-auth.sock}:/run/host-services/ssh-auth.sock
      # VDE 2026 Sovereign Bridge
      - ${VDE_HOST_SSH_AUTH_SOCK:-/run/host-services/ssh-auth.sock}:/run/vde-ssh.sock

    environment:
      - SSH_AUTH_SOCK=/run/vde-ssh.sock

    env_file:
      - ../../../../.env
      - ../../../../env-files/go.env

    networks:
      vde-net:
        aliases:
          - "go"
          - "vde-go"

    extra_hosts:
      - "vde-host:host-gateway"
      - "host:host-gateway"

networks:
  vde-net:
    external: true
