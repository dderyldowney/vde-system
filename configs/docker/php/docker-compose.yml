# Template for language VMs
# Variables: php, 2212, apt-get update -y {{INSTALL_CMD}}{{INSTALL_CMD}} apt-get install -y php php-cli php-xml php-mbstring php-curl composer
services:
  php-dev:
    build:
      context: ../../..
      dockerfile: configs/docker/base-dev.Dockerfile
      args:
        USERNAME: devuser
        UID: 1000
        GID: 1000
        PUBLIC_KEYS_DIR: /public-ssh-keys
    image: dev-php:latest
    container_name: php-dev
    hostname: php-dev
    restart: unless-stopped
    command: sh -c "apt-get update -y {{INSTALL_CMD}}{{INSTALL_CMD}} apt-get install -y php php-cli php-xml php-mbstring php-curl composer && /usr/sbin/sshd -D"

    ports:
      - "2212:22"

    volumes:
      - ../../../projects/php:/home/devuser/workspace
      - ../../../logs/php:/logs
      - ../../../public-ssh-keys:/public-ssh-keys:ro
      # SSH agent forwarding for VM->VM, VM->Host, VM->External communication
      - ${SSH_AUTH_SOCK:-/tmp/ssh-agent.sock}:/ssh-agent/sock:ro

    environment:
      - SSH_AUTH_SOCK=/ssh-agent/sock

    env_file:
      - ../../../env-files/php.env

    networks:
      - dev-net

networks:
  dev-net:
    external: true
