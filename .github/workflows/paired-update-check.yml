name: Paired Update Check
on:
  push:
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run paired update enforcer
        run: |
          zsh ./bin/paired_update_enforcer
          echo "Paired update check passed."
