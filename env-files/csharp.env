# Environment variables for C# development container
# 
# SECURITY NOTE: The postgres password below is for EXAMPLE/DEMONSTRATION ONLY
# For production: Generate secure random password with: openssl rand -base64 32
# Change the password below in this file AND in the generated docker-compose.yml
DATABASE_URL=postgresql://devuser:SuperSecretPassword123!@postgres:5432/csharp_dev_db
REDIS_HOST=redis
REDIS_PORT=6379
SSH_PORT=2226