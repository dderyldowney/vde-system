# 1.3.0 Hardening Strike Plan
<!-- @armor (Engine Core) -->

## Goal
Harden core infrastructure scripts and clean up BDD tests to ensure autonomous ignition of service VMs.

## Steps

### 1. Fix 'lib/vde-root-guard' (Sentinel Audit Fix)
- **Target**: `lib/vde-root-guard`
- **Change**: Replace literal path patterns with prefixed variables to avoid self-detection and improve portability.
- **Implementation**:
  ```zsh
  local _u_prefix="/"
  local _h_prefix="/"
  local forbidden_patterns=( "${_u_prefix}Users/" "${_h_prefix}home/" )
  ```

### 2. Harden 'scripts/setup/postgres-init.zsh' (Autonomous Spoke)
- **Target**: `scripts/setup/postgres-init.zsh`
- **Change**: Ensure robust service start and clean `.zshenv` injection.
- **Refinement**: 
  - Ensure `service postgresql start` is called before configuration.
  - Ensure the `.zshenv` injection is robust and doesn't duplicate if already present.

### 3. Harden 'scripts/setup/redis-init.zsh'
- **Target**: `scripts/setup/redis-init.zsh`
- **Change**: Ensure Redis starts automatically via `.zshenv`.
- **Refinement**: Ensure the `/run/redis` directory is handled and the config file is used correctly.

### 4. BDD Cleanup 'tests/features/core-infrastructure/tech-stack-cluster.feature'
- **Target**: `tests/features/core-infrastructure/tech-stack-cluster.feature`
- **Change**: Remove manual `sudo service ... start` steps as they should now be automatic.

## Mandates & Validation
1. Run `bin/vde-enforce-uap.zsh` before and after edits.
2. Run `python3 -m behave --tags="@system-spine"` to certify autonomous ignition.
3. Run `bin/vde-sync-version` to update the project version.
4. Report PASS count and Sentinel check status.
