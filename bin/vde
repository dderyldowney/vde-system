*** Begin Patch
*** Update File: /Users/dderyldowney/dev/bin/vde
@@
-# Load VDE root library to ensure robust root discovery across PATH invocations
-if [[ -f "$VDE_ROOT_DIR/lib/vde-root.zsh" ]]; then
-  source "$VDE_ROOT_DIR/lib/vde-root.zsh" 2>/dev/null || true
-fi
+# Load VDE root library to ensure robust root discovery across PATH invocations
+if [[ -f "$VDE_ROOT_DIR/lib/vde-root.zsh" ]]; then
+  source "$VDE_ROOT_DIR/lib/vde-root.zsh" 2>/dev/null || true
+fi
+
+# Load and run root-guard to ensure no hard-coded absolute paths leak at startup
+if [[ -f "$VDE_ROOT_DIR/lib/vde-root-guard.zsh" ]]; then
+  source "$VDE_ROOT_DIR/lib/vde-root-guard.zsh" 2>/dev/null || true
+fi
*** End Patch
PATCH