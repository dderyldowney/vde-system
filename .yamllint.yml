# VDE ARCHITECTURAL RECORD
# @shared-law (Infrastructure Foundation)
<!-- @shared-law (YAML Linter Configuration) -->
---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  indentation:
    spaces: 2
  comments:
    min-spaces-from-content: 1
